{
  "name": "techstore-diploma",
  "version": "1.0.0",
  "private": true,
  "description": "Веб-ресурс магазина компьютерной техники и комплектующих",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "db:push": "npm run db:push --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "setup": "npm install && npm install --prefix server && npm install --prefix client && npm run db:push && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
